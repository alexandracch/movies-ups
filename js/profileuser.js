$(document).ready(function(){
  // Iniciando parallax del gif de portada
    $('.parallax').parallax();
    // Iniciando boton para mostrar info de perfil
    $('.tap-target').tapTarget('open');
    $('.tap-target').tapTarget('close');
  });